# Core
fastapi==0.115.6  # Updated from 0.109.0 (CVE fix, includes starlette 0.41.2)
uvicorn[standard]==0.27.0
python-multipart==0.0.18  # Updated from 0.0.6 (CVE-2024-24762, CVE-2024-53981)
pydantic-settings>=2.6.1,<3  # >=2.6.1 REQUIRED by mcp>=1.2 (which owns
                              # mcp.server.fastmcp). The old ==2.1.0 pin is what
                              # forced pip outside the working mcp range and made
                              # tests/unit/test_mcp_server.py skip silently while
                              # the published package was broken. See OPEN_WORK 2026-08-04.
aiofiles==23.2.1
slowapi==0.1.9  # Rate limiting middleware

# Payments
stripe==10.8.0

# Database
sqlmodel==0.0.14
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9  # Required for sync database sessions
redis==5.0.1

# ML & Processing
sentence-transformers==2.3.1
onnxruntime==1.20.0
scikit-learn==1.5.0  # Updated from 1.3.2 (PYSEC-2024-110)
spacy>=3.8,<3.9  # Phase 5: Government API domain classification
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl  # spaCy English model

# Auth
PyJWT[crypto]==2.8.0  # JWT validation with JWKS support
# THIS PIN CHOOSES OUR MCP SDK VERSION. It is not only an HTTP client pin.
# `httpx==0.27.0` (the old value) silently capped `mcp` at 1.12.4, because mcp
# 1.13.0 raised its floor to httpx>=0.27.1 and 1.12.4 was the last release
# accepting >=0.27. Nobody decided that; a pin in the auth section did, and the
# comment at the MCP mount in main.py asserted the opposite for months.
#
# Consequence that was already live: 1.12.4 has no `Icon` class and no
# `website_url` on the low-level server, so the serverInfo work of 2026-08-06
# was INERT on the hosted transport -- production's initialize response carried
# neither. It also meant the two transports ran different SDKs, since the
# published tru8-mcp package declared only httpx>=0.25.0 and resolved 1.29.0.
#
# Held below 0.28 deliberately: 0.28 dropped the deprecated `proxies=` and
# `app=` arguments. We use neither (checked), so this is headroom, not a need.
# Verified by dry-run: httpx 0.27.2 + mcp 1.29.0.
httpx>=0.27.1,<0.28
svix==1.30.0  # F-AUTH-02: Clerk webhook signature verification

# Content Processing
trafilatura==1.6.3
readability-lxml==0.8.1
bleach==6.1.0

youtube-transcript-api==0.6.1
Pillow==10.3.0  # Updated from 10.2.0 (CVE-2024-28219)
beautifulsoup4==4.12.3  # Fact-check parser HTML extraction
tldextract==5.1.1  # Phase 3: Domain credibility framework
weasyprint==62.3  # PDF report generation (modern CSS support)
pydyf>=0.10.0,<0.12  # WeasyPrint PDF backend (0.12 has breaking API change)
jinja2==3.1.6  # Template engine for PDF reports
PyPDF2>=3.0.0  # Phase 2: PDF page extraction
pdfplumber>=0.10.0  # Phase 2: PDF text extraction with page numbers

# MCP (Model Context Protocol)
# Floor raised 1.2 -> 1.12 (2026-08-06) to match tru8_mcp/pyproject.toml: the
# API imports tru8_mcp.server to mount the hosted transport, and that module
# now passes `annotations=` to @mcp.tool() -- a TypeError at import, i.e. no
# boot at all, on an SDK that predates the keyword.
mcp[cli]>=1.29,<2  # H-API-07: MCP server for AI agent discovery.
                  # UPPER BOUND IS LOAD-BEARING: mcp 2.0.0 removed
                  # mcp.server.fastmcp, which tru8_mcp/server.py imports.
                  #
                  # FLOOR raised 1.12 -> 1.29 (2026-08-10). 1.29.0 is the final
                  # 1.x feature release; 1.x is maintenance-only from here, so
                  # this is the end of the line we can reach without migrating.
                  # It puts us above three High-severity advisories (the newest
                  # fixed in 1.28.1) -- we are not exposed to any of them, but a
                  # floor that PERMITS a vulnerable resolution is free to close.
                  # It also activates the `Icon`/`website_url` serverInfo fields
                  # that 1.12.4 silently ignored.
                  #
                  # Read the httpx pin above before changing either: they
                  # jointly determine which SDK the image runs.

# Storage (S3/MinIO)
boto3==1.34.0

# Monitoring
sentry-sdk==1.45.1  # Updated from 1.39.1 (CVE-2024-40647)
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-instrumentation-httpx==0.43b0
opentelemetry-instrumentation-sqlalchemy==0.43b0
opentelemetry-exporter-otlp==1.22.0
prometheus-client==0.19.0

# Push Notifications
exponent-server-sdk==2.0.0

# Email Notifications
resend==2.19.0
