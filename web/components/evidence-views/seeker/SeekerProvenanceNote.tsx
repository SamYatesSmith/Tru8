export function SeekerProvenanceNote() {
  return (
    <div className="border-t border-zinc-100 pt-4 mt-6">
      <p className="text-[11px] text-zinc-400 leading-relaxed">
        Element states and evidence gaps are derived from AI analysis.
        Re-search uses 1 credit per element to find additional sources.
        Research briefs are optional and help refine search queries.
        Related claims are matched by shared entities — no user data is exposed.
      </p>
    </div>
  );
}
