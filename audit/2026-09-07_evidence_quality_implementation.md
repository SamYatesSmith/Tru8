# Evidence quality implementation — 7 September 2026

User authorised implementation of the hands-on improvement map, with a commit after each validated step. Baseline: `47664a1`. Branch: `codex/evidence-quality`. Existing uncommitted commercial assessment and register changes must not be included in implementation commits.

## Sequence and status

**Final regrade 9 September 2026 (late):** after the evening's fixes (extraction restore, absence-of-evidence gate, question labels, page-level source exclusion, decomposition rule) Astra's 14 inputs read **about 8/10 by Claude's grading** on the default path; Astra's own gate (human review, usability, stall trace) remains unmet. Details in the regrade record.

**Regrade 9 September 2026 (evening):** [Astra's 14 inputs re-run in both arms](2026-09-09_astra_regrade.md) — default ≈ **7/10**, candidate not ready. Three inputs lost their contested conjunct at extraction; `restore_single_thesis` now restores the user's sentence verbatim when one claim comes back missing a content token. 8/10 is not claimed.

**Ownership changed 9 September 2026:** the founder handed the branch to Claude after a three-reviewer audit. Founder decisions and the phase plan live in the [takeover record](2026-09-09_takeover_stabilise.md) and `OPEN_WORK.md` START HERE. Astra's original proposal: `tmp/tru8-hands-on/tru8-route-to-eight.md`.

Latest checkpoint: [quantitative result guard](2026-09-09_quantitative_result_guard.md) (candidate-only). A `compatible` support on an element stating a figure must quote that figure in an accepted form (percentage variants, ratio equivalents) or it becomes `unknown/result` → context, with the model's decision kept in the receipt. Closes the frozen SELECT pair mechanically; 47 scope-review tests pass through the real parser; fingerprint v11. Paid run 1: 39/42 — the pair and all result controls passed; the three misses were null-result challenges demoted on a model `mismatch/result`, closed by a second rule (`contrary_result_is_the_challenge`). Paid run 2: **42/42**, ~4p. Review completeness: the scope review runs as concurrent calls of ≤6 pairs under separate 25 s bounds (a slow call loses only its own pairs, disclosed in `receipt.calls`). Study independence: [same-study scope gate](2026-09-09_same_study_scope_gate.md), default-on — hosts of one DOI/PMID/PMC count once per side of an element, receipt names the counted carrier; placed before echo. [Integrated run 3](2026-09-09_integrated_run3.md) on the final phase-2 code: three live reports completed, identities agree, all SELECT supports quote the figure, creatine contextual, split review exercised. Step 8 delivered as [passage-backed PDF export](2026-09-09_pdf_passage_export.md) (export only, founder-scoped). Remaining: step 9 (held-out benchmark, human review, reliability runs, stalled-check trace) and merge readiness.

Previous checkpoint: [takeover stabilise](2026-09-09_takeover_stabilise.md). Fixed and pinned: strengthening refunds now write `research_refund` (a second failed operation on one check violated the ledger's one-`refund`-per-check index and stranded the operation — the test fixture now carries that production index); 21 existing serialisation tests broken by an unguarded `text_provenance` read; `archived_url` excluded from report snapshots (Wayback writes it after completion, failing any strengthening started in that window); `/verify/{id}/revisions/{rev}` returns the signed fields the page prints; public "System interpretation" text gated fail-closed; Seeker no longer treats a review that never ran as unknowns. Whole backend suite, web suite (195), TypeScript and default replay 185/1/13/2 (zero drift) passed — figures in the record. Candidate flags remain OFF; no deployment.

Previous checkpoint: [result fidelity correction](2026-09-09_result_fidelity.md) (Codex, committed 9 September by Claude with its doc). The candidate's recital fallback now exempts an explicitly reported study finding; final scope review requires the complete result with effect measure and can select exact source lines for context corrections; passage fingerprint v10. Fixed-source evaluation 38/38 across two repeats; default replay 185/1/13/2. Remaining failure pair frozen in `qualitative_effect_failures.json` (two SELECT sources supporting the full 20% assertion on qualitative MACE text). Integrated re-review: Venus timed out at its 25 s bound, SELECT 12/15 pairs assessed.

Previous checkpoint: [integrated candidate and broader scope pilot](2026-09-08_integrated_candidate_pilot.md), `7d7c986`. Three local integrated reports completed in 40–47 seconds with all 54 ledger sources retained and matching owner/public identities. Broader controls 20/24; candidate activation and the 8/10 gate blocked by evidence-fidelity failures.

Previous checkpoint: [public/export evidence-snapshot identity](2026-09-08_report_identity.md).
Owner/public/PDF views now identify their loaded evidence snapshot and match retained
revision content rather than selecting the newest history row. Historical verification
is revision-specific; unsigned/unmatched content is disclosed. Backend 75 checks,
web 186 plus final affected checks, TypeScript, visual PDF review and the unchanged
185/1/13/2 replay baseline passed. Public/export local implementation is complete;
live acceptance and the remaining quality plan are not. No deployment or flag activation.

Previous checkpoint: [owner revision viewer and retry header fix](2026-09-08_revision_viewer.md).
Owners can inspect and compare retained strengthening snapshots without replacing
the current report. The API client now transmits its supplied idempotency key.
TypeScript, 183 web tests and 10 PostgreSQL integration checks passed. This closes
local viewer implementation only; public/export identity, live acceptance and the
remaining semantic/operational work are still open. No deployment or flag activation.

Previous checkpoint: [explicit date-anchor gate](2026-09-08_fact_anchor_gate.md).
The disabled candidate now passes 27 targeted conditions across 13 fixed-source
cases after correcting a measured planned-change regression. Initial mapping and
passage additions share the gate; EVIDENCE/GAPS explain exclusions. This narrow
date/lexical prerequisite does not complete general semantic applicability or
authorise rollout. Backend 97 checks, full web 173 plus final affected checks and
TypeScript passed; final replay matched 185/1/13/2 with zero cassette drift.

Previous checkpoint: [element/time applicability candidate evaluation](2026-09-08_applicability_evaluation.md).
Twenty additional fixed-source arms preserve positive controls and remove two observed
errors in the combined arm, but three historical-applicability assertions still fail.
The shared prompt candidate remains behind the OFF passage flag (contract v3).
Hash-bound offline semantic checks now distinguish this failure from successful
processing/quotation validation. 44 backend checks and the exact 185/1/13/2 replay
baseline passed. Step 5/6 semantic acceptance remains open; no deployment.

1. **Locally verified:** truthful Gaps coverage and review status. Contextual/disputed material remains visible; evidence presence never implies all questions are settled. Web suite: 151 passed; TypeScript check passed.
2. **Locally verified:** Compare relationship semantics and display identity. Context/directional pairs are contextual; identical directional mappings say “same direction”, not whole-source agreement. Selected A/B order preserves summaries, text receipts and one-sided rows while the cache remains canonical. Fresh-read versus saved-map distinction is visible; extraction is no longer labelled “full article”. All nine relationship combinations, missing mappings, live recomputation and reversed display covered: 20 backend tests and 4 web regressions passed; TypeScript passed. Existing API `aligned` responses containing context render honestly during rollout.
3. **Locally verified:** research completion explicitly fetches and replaces the client report with a fresh token; failures retain the old report and expose retry, and older overlapping responses cannot overwrite newer results. Compare reloads saved mappings after report changes. The source panel exposes each relationship and its saved reasoning as “System interpretation”, explicitly discloses unavailable exact passages, and receives focus/scroll on opening. Claim-wide explanations preserve their own claim/element descriptions. Web suite: 158 passed; TypeScript passed. Production UI acceptance remains outstanding.
4. **4a and 4b locally verified:** canonical evidence metadata survives strengthening; selected elements share one retrieval/classification/distillation/mapping operation. Durable check-level admission serializes report writers, deduplicates retries before debit and refunds an interrupted operation once. Atomic completion preserves before/after report snapshots and their signatures. PostgreSQL concurrency, cancellation/timeout, changed-report rollback, signing failure, revision ownership and migration/bootstrap parity are tested. Web polling pins operation identity, avoids overlapping polls and counts bundled sources once. Full replay reproduces **185 ok / 1 warn / 13 known fail / 2 unexercised, zero cassette drift**. Revision-change presentation and production acceptance remain open.
5. **5a locally verified; 5b implemented behind a disabled rollout setting:** retained element-selected passages can now feed distillation and a bounded source/element review. Exact quoted text, IDs, offsets and captured-version identity are checked before a new relationship is accepted. The UI distinguishes linked quotations from interpretations and exposes conflicts/uninspected pairs. Real-model quality evaluation and activation remain open; Step 5 acceptance is not complete.
6. **6a and 6b capture contract locally verified:** publication precision survives fresh capture/storage/remapping; explicit effective-date/as-of wording is retained with exact context and visible unreviewed status. Semantic fact/interval linking, current-value evaluation, decomposition and source-role refinements remain pending.
7. Pending: operational reconciliation and end-to-end acceptance.

No production deployment or production-log inspection has been performed. A bounded paid model evaluation has now run on public/synthetic fixed sources (see the latest checkpoint). Local verification is not an 8/10 re-score; live evidence and reviewer evaluation remain separate acceptance gates.

## Resume checkpoint — 8 September 2026

### Integrated model evaluation checkpoint

- After `bc3119c`, user requested proceeding with the integrated evaluation. Prepared and executed a bounded 2×2 comparison using the existing configured models, 16 real/synthetic cases, identical source bytes and frozen shared classifications. Fixed element descriptions isolate distillation/mapping; this does not evaluate decomposition or live retrieval. No application data or persistent settings changed.
- Initial live sweep was stopped after 18 completed arms: of 8 passage-enabled reviews, 2 returned the original element JSON shape and were safely rejected, 3 were partial and 3 complete. The prompt carried both the original element schema and the new pair schema. Preserved these initial results; they are not mixed with corrected results.
- Fixed the actual contract: reuse relationship judgement rules without the competing element JSON instructions, use a dedicated `passage_review` call label and provider response schema, retain exact quote/pair validation unchanged. Enabled passage fingerprint becomes v2; default-off fingerprints and old mapping prompts remain unchanged. No model choice changed.
- Corrected sweep completed all **64 arms**. Among 32 passage-enabled arms: 15 complete, 15 needs_review, 2 partial; these statuses describe bounded processing, not accuracy. 40 retained citations pass exact-text/source-version checks, 21 pair disagreements are exposed, 2 pair rows are rejected. SQLite exception/read-only mappings are more consistently recovered in this small sample; unrelated controls stay unmapped.
- **Do not activate:** a rate-level article is added as a challenge to the separate decision-date element in the combined Bank case; page-clock dates contaminate temporal interpretation in real and fictional controls. Exact quotations do not guarantee entailment. SELECT review is limited to FDA text because the trial abstract capture failed. No global accuracy or 8/10 re-score is claimed.
- Validation: 42 backend checks including provider-schema wiring, existing completion/model metadata, exact quotation rejection, actual PostgreSQL passage persistence, evaluation hash isolation/dry-run/request-budget controls. Default-off replay `tmp/quality-replay-passage-schema.log`: 185 ok / 1 warn / 13 known fail / 2 unexercised, zero cassette drift. No golden changes.
- Local responses: `tmp/passage-factorial-evaluation` (interrupted initial sweep), `tmp/passage-factorial-corrected` (complete sweep). Aggregate recorded provider usage: 331,099 input / 42,706 candidate-output / 68,348 thinking tokens (442,153 total), including 188 saved responses across both sweeps. An interrupted in-flight request may not be represented; this is usage telemetry, not a billing reconciliation. Both flags remain OFF, no deployment.
- **Next, original-plan findings 5/6:** general element-specific entailment and temporal-applicability controls; make page/header observation distinct from the effective date of the cited fact. Replay these failures plus unseen value/date seeds and unrelated source layouts before activation. Details: [factorial evaluation](2026-09-08_passage_factorial_evaluation.md).

### Structured extraction checkpoint

- After evaluation-preparation commit `63e89ea`, user authorised resolving the measured extraction issue while staying within the original improvement plan. This implements the extraction-coverage part of original finding 4: source content was missing before distillation/mapping. No publisher-specific selectors, values, current answers, or AI triggers were added.
- `ENABLE_STRUCTURED_EXTRACTION=False` is separately gated. Enabled extraction supplements normal narrative with omitted compact numeric value/label groups, small tables (captions/header rows preserved) and definition lists inside main/article regions. Hidden controls, navigation, footer/header/form content are ineligible for supplementation. Maximum 12 blocks / 6,000 added characters; oversized blocks are skipped whole. Existing narrative is retained on failure; no claim of complete extraction.
- The enabled contract has a distinct retrieval-cache namespace and pipeline fingerprint. Default-off keys/fingerprints remain compatible; old cached snippets cannot mask activation. The existing source-detail UI receives recovered retained passages through the same receipt/API contract; no new frontend state or date interpretation is introduced.
- Frozen-byte audit: Bank homepage 668→761 characters, with the missing rate/decision block retained for both opposing cases. FDA 4,761→4,800 (publication block recovered, not an effective date); both SQLite pages and the comparison article unchanged. All five original narrative prefixes preserved; source hashes checked. SQLite's late BUSY exception was already present in retained passages, confirming a separate downstream mapping evaluation need rather than another extraction edit.
- Backend 98 checks passed, including generic non-financial widgets, tables/units, hidden/navigation exclusion, signed-number dedup, bounds, fallback, switch/cache/fingerprint isolation, receipt/API persistence, dates, distillation and strengthening. Full replay `tmp/quality-replay-structured-extraction.log`: expected **185 ok / 1 warn / 13 known fail / 2 unexercised**, zero cassette drift. No cassette/golden edits. Details and limitations: [structured extraction checkpoint](2026-09-08_structured_extraction.md).
- Next: classify once and freeze shared evidence, then compare integrated model paths on identical saved sources plus generated/held-out controls. Current applicability, false directional additions, underlying-study grouping and decomposition remain open. Do not claim measured research-quality improvement or activate based only on extraction tests.

### Evaluation preparation checkpoint

- After `3a5894b`, user chose evaluation preparation and clarified that no maintained values, institution-specific runtime rules or AI triggers are acceptable. Added only test tooling/specifications/docs, with 8 real-source cases and 8 generated fictional value/date controls. No application code change or model execution.
- Public capture and offline extraction completed. Five of six pages available; trial abstract HTTP 203 is recorded as unavailable. These are new snapshots, not original production inputs. Reviewer guidance is separated from model inputs; source classifications and both model arms remain unrun.
- The saved homepage's current-value/decision widget is absent from normal extraction, which instead retains a rate in a news headline. General extraction coverage is therefore the next concrete implementation priority before model evaluation; no publisher-specific value rule is proposed.
- Three preparation checks passed. Full replay `tmp/quality-replay-evaluation-pack.log`: expected **185 ok / 1 warn / 13 known fail / 2 unexercised**, zero cassette drift; no golden changes. Full checkpoint and local pack location: [evaluation preparation](2026-09-08_passage_evaluation_preparation.md).

### Step 6b checkpoint

- Prior commit: `d49489a`. Additive `text_provenance.temporal` version-1 receipt uses the existing JSONB column; no migration, model or prompt change. Capture remains fresh-extraction-only and never backfills an old receipt. `ENABLE_PASSAGE_MAPPING=False` remains unchanged.
- Publication receipt preserves the value supplied at capture, date provenance and known precision (year/month/day/timestamp). Already-normalised date objects explicitly have unknown original precision. On strengthening, `evidence_for_mapping` restores a supplied string from the receipt instead of letting the database's January-1 expansion acquire month precision. The source UI uses that same saved string. This does not restore raw metadata discarded before capture, and snippet-only legacy records retain their old contract.
- `services/temporal_provenance.py` scans only retained passages for explicit `effective from/on/until`, `in force from/until`, and `as of` followed by a valid complete date (ISO or English full month names). Up to 12 candidates carry a date role, normalised stated date, exact contextual sentence citation, extraction hash and Unicode offsets. Extra candidates are counted. A homepage clock, publication/capture timestamp, invalid date or bare `current` wording cannot establish an effective date.
- Candidates are **unreviewed**, including historical, negated, hypothetical and planned statements. Sentence context retains negation rather than quoting only the positive date phrase; surrounding retained passages are available in the UI. Roles are lexical observations, not established fact intervals. Separate start/end mentions are not automatically paired, and no date/domain overrides a support/challenge relationship or changes state arithmetic. Applicability remains `unestablished` for every receipt in this phase.
- Frontend Time applicability section validates each quotation against the captured version before displaying the candidate date, shows missing-basis and bounded-scan notices, explains that a start date does not prove continued validity, and leaves legacy records without fabricated temporal findings. This is available through the shared source-details component.
- Validation: 105 backend checks covering receipts, dates, distillation, symmetric gates, initial PostgreSQL persistence, strengthening, revision signing and passage mapping; 171 web tests and TypeScript passed. After tightening quotation context, all 18 temporal-receipt cases passed again. Test source text is synthetic. Full replay `tmp/quality-replay-step6b.log`: **185 ok / 1 warn / 13 known fail / 2 unexercised**, zero cassette drift. No cassette/golden changes; the exit code reflects the established known failures.
- **Next:** freeze the actual Bank Rate/SQLite/SELECT source inputs and evaluate semantic fact-to-date linking on identical pools before enabling passage-aware processing. Bank controls must include expired official text, newer contrary evidence, negated/planned dates and undated material in both directions. Evidence capture is not the full current-applicability fix. Raw metadata preservation before capture, snippet-only date precision, semantic intervals and user/reviewer acceptance remain open. Decomposition and source-role/underlying-study grouping can proceed as separate code phases.

### Step 6a checkpoint

- Step 5b commit: `55a74eb`; its rollout remains disabled. This checkpoint tightens the existing temporal resolver without changing model configuration, prompts, source weights or introducing a migration.
- The temporal resolver now accepts publication strings with an explicit valid month/date instead of using the general date parser's year-to-January fallback. Year-only, malformed and unknown strings cannot resolve a bare source month. Already-normalised datetime objects have lost their original precision; this patch cannot recover that information. Preserving raw date precision across ingestion/storage remains part of the temporal contract work.
- Bare-month resolution abstains in sentences carrying forecast/target/expectation/future markers, including interval endpoints. It does not assign a future year instead. Explicitly stated years remain readable and retrospective sentences elsewhere in the source remain eligible. The lexical guard is conservative: retrospective discussion mentioning expectations/targets can also abstain. This is not a tense classifier or proof of current applicability.
- Source details label publication separately from text capture, preserve year-only/month-only date display and explicitly explain that neither establishes when a value applies. Old records without a date say it is unavailable. No effective date is invented from a homepage clock or a capture timestamp.
- Validation: 102 backend checks across temporal parsing, symmetric mapping gates, passage mapping and replay signals; 167 web tests and TypeScript passed. New cases cover incomplete/invalid publication dates, future-facing sentences, explicit dates, sentence-local resolution and preservation of both support and challenge when year inference abstains. Full replay `tmp/quality-replay-step6a.log`: expected **185 ok / 1 warn / 13 known fail / 2 unexercised**, zero cassette drift. No cassette/golden changes. The nonzero bench exit reflects the established known failures, not a clean all-pass corpus.
- **Next (6b):** preserve raw publication precision and introduce passage-linked fact applicability (effective interval or explicit as-of/current statement) separately from publication and observation. Validate against frozen Bank Rate sources, including an outdated official source versus newer evidence. Unknown applicability must be visible symmetrically; domain identity alone cannot establish currency. The current patch deliberately does not claim to resolve the motivating Bank Rate dispute. Decomposition and underlying-study/source-role refinements follow separately.

### Step 5b checkpoint

- Prior Step 5a commit: `15a461b`. New `ENABLE_PASSAGE_MAPPING=False` is deliberately OFF by default and was not enabled in any deployed or persistent local configuration. The change affects model input; response-stub tests and the old replay corpus cannot establish its accuracy. No migration or paid model call is required for this code checkpoint.
- When enabled, distillation uses the retained element-selected windows (same 8,000-character input ceiling) plus explicit element descriptions. This replaces the leading slice for sources with valid capture receipts. Sources without those receipts keep the legacy input. Both initial research and strengthening use this contract.
- `services/passage_mapping.py` runs after the existing whole-source completion backstop. It selects up to 12 source/element pairs, rotating across elements and prioritising absent references; each gets at most two 900-character passages. A source already mapped elsewhere remains eligible. One additional model call per eligible claim, 20-second ceiling, 4,800 output-token ceiling. The batch completion wrapper allows 50 seconds with the rollout on (legacy default remains 25).
- A returned relationship requires the exact supplied passage ID and a nonempty quotation which occurs literally in that passage; stored citations carry Unicode offsets and the extraction fingerprint. Normalised/invented/foreign-passage quotes, duplicate pair responses and missing responses cannot establish a new reference. Text-cleanup serialization now preserves capture/citation payloads byte-for-byte in their Unicode representation.
- Exact-text validation does **not** establish entailment, source truth, complete extraction or whole-document coverage. V1 manifests retain their existing signing scope; citations are narrative metadata, not newly signed canonical fields. The enabled passage contract is represented in the pipeline fingerprint; default-off fingerprints remain unchanged.
- Existing relationship disagreements are recorded as conflicts, never silently overwritten; an `unrelated` excerpt does not erase a prior whole-source relationship. Pair receipts distinguish candidate/selected/assessed/uninspected counts and failed/partial/conflicting responses. Counts cover only lexical matches in retained passages, not every possible source/element pair. Gaps and Evidence expose incomplete/conflicting review; desktop and mobile source details resolve quotations against the correct captured source version and preserve the unavailable-citation disclosure otherwise.
- New references use the existing symmetric scope gates and state derivation. Gates inspect reviewed passage context rather than a stale leading snippet; later scope/recovery passes also recover cited context. No source weighting, floor, gate order or relationship-count policy was changed.
- Validation: **176 backend tests**, covering passage contracts, old mapping/completion/grounds/receipt behaviour, signing/fingerprint compatibility, and real PostgreSQL strengthening/revision persistence. **165 web tests and TypeScript passed.** SQLite-style and rate examples in new tests are synthetic fixed-text fixtures, not newly fetched official documents. They verify code behaviour under supplied responses, not model correctness. Default-off replay: `tmp/quality-replay-step5b-final.log`, expected 185/1/13/2 baseline with zero cassette drift; no cassette/golden changes.
- **Next acceptance work:** obtain and freeze the actual SQLite, Bank Rate and SELECT source extractions from the motivating cases, then compare legacy and passage-aware model outputs on identical source pools. Review quotation validity, source/element recall, false directional additions, temporal scoping, conflicts, latency and token use. Include reversed/opposing examples and a no-relationship control. Do not enable based only on passing mocks or re-record cassettes to conceal input drift. Activation/paid evaluation and production deployment require their separate authorised scope. Step 6 implementation can proceed independently.

### Step 5a checkpoint

- Prior step 4b commit: `0b56b52`. New nullable `Evidence.text_provenance` JSONB column, migration `text_provenance` after `research_operations`; the additive migration was exercised against disposable PostgreSQL schemas and applied locally. Existing rows remain NULL. No production migration or deployment.
- `app/services/text_provenance.py` retains at most eight 900-character windows selected lexically across the full available extraction, rotating across element descriptions. Original Unicode slices are copied directly; offsets are extraction character positions, **not PDF page/HTML locations**. Full extraction SHA-256 and total/retained lengths identify the captured version. Capture time records retention in this pipeline run, not publication/effective date. The complete document is not stored and extraction completeness is not asserted.
- Capture runs before classification/distillation cleanup on initial research and strengthening, and also at the distiller boundary. Exact windows and the original snippet survive successful or failed distillation; model-generated facts are stored separately. The initial save path and generic evidence round-trip both preserve receipts. No receipt is manufactured for snippet-only or old records.
- Shared `finalize_distilled_payload` runs after classification so snippet-first mapping and persistence consume successful distilled facts rather than stale snippet content. This avoids changing the classifier's input while it runs concurrently. Re-search uses the same helper.
- Source-detail UI renders only retained extraction text as blockquotes, separates original snippets/generated facts, and explicitly states that excerpts have **not** been linked to the relationships. Old records retain their unavailable-passage disclosure. The current v1 manifest still has its existing limited signing scope; it does not sign this new narrative payload.
- Validation: 50 focused backend checks (including actual initial persistence, migration without backfill, research transaction regressions, distillation and provenance), plus five existing initial-save tests; all 162 web tests and TypeScript passed. Fixed-document tests retain late SQLite exceptions and a later rate-table block within 7,200 characters and verify every slice/hash, including Unicode. This tests retention, **not mapper quality or live Bank/SQLite outcomes**. An additional broad runner smoke invocation was interrupted while stalled after partial progress; it is not counted as passing.
- Full replay `tmp/quality-replay-step5a.log`: **185 ok / 1 warn / 13 known fail / 2 unexercised**, zero cassette drift. No cassette/golden changes. This bench does not establish the quality of the newly retained excerpts or expand the distiller's current 8,000-character leading input.
- **Next (5b):** use retained element-directed passages in a bounded model-input contract; require exact passage IDs/text for relationship citations; record inspected passage/element pairs and revisit relevant already-mapped documents for uncovered elements. Compare extraction receipts and passage-aware completion require their own fixed-source evaluation. Do not claim the current lexical matches are verified citations or that the old whole-source completion pass is comprehensive.

### Step 4b checkpoint

- Step 4a committed as `c52aa9c`. Step 4b introduces `research_operation` and `report_revision`, exported for fresh-database bootstrap, with migration `research_operations` after `claim_comparison`. Apply the migration before starting the changed API in an existing environment. It was exercised up/down in disposable local PostgreSQL schemas, then applied to the local development database (confirmed at `research_operations (head)`); no production migration was run.
- New strengthening admission is one active operation per **check**, because one manifest spans all claims. A request key can be retried without another debit; a competing distinct request receives 409 before billing. External work holds no database transaction. Completion locks the check and target claim and verifies the baseline hash before saving evidence, map, both snapshots and the new manifest together.
- Failures/cancellations refund the operation's original debit, leaving the completed parent check usable. A 30-second reconciliation loop repairs expired pending/running operations, including process interruptions. It never restarts paid work automatically. No new sources is a completed research attempt, not an automatic refund.
- Owner-authenticated revision list/read endpoints retain full stored report snapshots. Public historical verification returns only verification results and IDs. Existing v1 signature semantics are preserved: these signatures cover the existing canonical decision fields, **not every narrative word or complete page text**. Old unsigned reports stay unsigned when signing is disabled; an already signed report cannot silently become unsigned.
- Validation: 44 focused backend tests across transaction/migration, re-search payload/distillation, credit/path separation and watchdog suites; 161 web tests and TypeScript passed. The SQLModel runtime initially omitted bounded VARCHAR lengths on fresh bootstrap; explicit column types fixed the discrepancy and parity now passes. New retrieval IDs are checked against existing IDs before mapping. Retry retains its key after an uncertain response, including the UI's reset path.
- Replay: `tmp/quality-replay-step4b.log`, exact 185/1/13/2 baseline. No cassette/golden updates or paid calls. Existing dependencies `aiofiles` and `python-multipart` were missing from the local test runtime and installed there.
- **Next:** step 5 passage/extraction provenance and mapping coverage. A user-facing per-relationship revision diff/history viewer, public/export revision identity and live end-to-end evaluation remain acceptance work; the API preserves history but does not yet provide that viewer. No deployment or new quality score is claimed.

### Earlier Step 4a checkpoint (historical)

- Completed commits: step 1 `4d68057`, step 2 `302b8cd`, step 3 `ba9df3d`.
- Web: all 158 tests passed, TypeScript passed. Backend Compare categories/prompt/budget plus evidence payload: 35 tests passed. Formatting completed for the prepared Python files.
- Step 4a files: `backend/app/services/evidence_payload.py`, `backend/app/pipeline/re_search.py`, `backend/tests/unit/test_evidence_payload.py`, `backend/tests/unit/test_re_search_payload_integration.py`. Existing unrelated `.gitignore`, commercial assessment and older OPEN_WORK additions remain separate.
- **Local test environment repaired:** the user restored Docker Desktop; `docker compose up -d postgres redis` started the existing project services successfully. No daemon reset, WSL shutdown, database deletion or production action was performed by this task.
- Test runtime: `tmp/quality-venv/Scripts/python.exe`. The initial replay after Docker recovery had drift across all ten claims. A diagnostic exposed missing `sentence_transformers` (fallback to word overlap) and `pdfplumber`; after installing the declared dependencies and caching MiniLM, the diagnostic reproduced 40/40 responses and the whole corpus returned to the exact accepted baseline. Do not mistake the initial environment failure for a code regression or regenerate cassettes to hide it.
- Reproduce from backend with process-local `HF_HOME=C:\Users\projects\Tru8\tmp\quality-hf-cache` and `HF_HUB_OFFLINE=1`, then `..\tmp\quality-venv\Scripts\python.exe scripts/replay_bench.py --all`. Model download is not needed for subsequent runs. Runtime package versions: `tmp/quality-test-requirements.txt`. Full replay log: `tmp/quality-replay-all.log`. No golden/cassette updates or paid model calls were made.
- Latest focused backend run: 36 tests passed (Compare categories/prompt/budget, payload helpers and re-search integration); prior web run remains 158 passed plus TypeScript. Whole step 4 and steps 5–7 are not complete.

## Step 4 design constraints recovered from the persistence path

1. Both bulk endpoints (`research-gaps`, `research-thin`) currently launch one whole-map writer per element. Replace with one operation that plans targeted retrieval for the selected elements, merges once, maps once and commits once. Single-element research must use the same path.
2. Protect against concurrent operations across processes, not just an asyncio lock. Admission/idempotency must happen before debit; use a durable operation identity for status, retries and refunds. Serialize writes or compare a stored revision, and test competing completions against PostgreSQL.
3. Preserve existing evidence metadata and assign candidate identities before mapping (prepared patch). Run the same extraction/distillation contract for fresh candidates; preserve claimant/jurisdiction metadata from the existing map.
4. **Signing issue confirmed by inspection:** `re_search.py` changes claim maps/evidence without archiving a prior revision or replacing `Check.manifest`. `/verify` hashes the current map/evidence, so a changed signed payload no longer matches its original manifest. Store an immutable prior snapshot and manifest, then sign the coherent updated report in the same transaction. Coordinate at check level because one manifest covers all claims. Never merely re-sign and erase the old record.
5. Bundle status and watchdog cancellation must terminate every selected element; counts must not multiply the same merged sources by the number of elements. Success is published only after commit. Failure rolls back evidence/map/signature together and refunds the operation's debit once, without failing the already-completed parent check.
6. Required acceptance: duplicate requests, simultaneous element runs, timeout/cancellation before commit, signing failure, preserved source weights/dates, signed prior snapshot verification and current revision verification. The local automated checks now cover these (Step 4b checkpoint above). Live and user-facing revision review remain open.

The detailed original proposal remains at `tmp/tru8-hands-on/tru8-route-to-eight.md`; steps 5–7 and human/live acceptance have not been completed.
