# Source role and concentration — progress toward 8/10

The user's original assessment was 6/10; the minimum target remains 8/10. No new numerical score has been demonstrated. Infrastructure tests, source counts and individual fixes are not score increments.

## Change

The live SQLite acceptance exposed the official WAL documentation being changed to commentary/analysis solely because its domain exceeded a pool-share threshold. The old rule ran after mapping and changed tier, evidence type and classification method without recomputing the map's recorded basis. Removed this automatic relabelling. Classifier output now survives the final assembly step.

Initial research and strengthening record `metadata.source_concentration`: counts of mapped documents by normalised domain, with independence explicitly unestablished. Source records are neither deleted nor capped. Evidence/Gaps views disclose repeated publishers separately from source roles. Counts are derived from the current map on strengthening, so old receipt statuses do not determine the new summary.

This is not a new claim that model classifications are always correct, nor a study-level independence algorithm. Multiple documents from one publisher may or may not be independent; multiple publishers can repeat the same underlying study. No website exceptions, maintained factual values or model changes were introduced. Existing reports and retained signatures are not rewritten. Previously overwritten classifications cannot safely be reconstructed from their present values; new research is needed for those records.

## Acceptance gates

| Area | Evidence so far | Still required for 8/10 |
| --- | --- | --- |
| Preservation and revision integrity | Live local 19→32 source run preserved originals; owner/public identity matched; both revisions verified | Browser/HTTP/worker interaction and operational acceptance |
| Decisive evidence mapping | Retained passages expose the official exception; default distillation still omits it | Fixed-input candidate/control evaluation and broader held-out review before activation |
| Source roles and independence | Domain share no longer overwrites classification; repeated publishers disclosed | Study/observation independence and classifier accuracy review |
| Decomposition and output coherence | Earlier cross-view fixes implemented | Eliminate vacuous/overlapping premises and review representative outputs |
| Export and usability | Real 32-source PDF rendered and inspected | Split-source pagination and isolated footer fixes, then browser review |

The original proposal's broader held-out corpus and human re-score remain the standard. One SQLite example cannot establish an 8/10 product rating.

## Validation

Twenty focused backend checks passed, covering role preservation, concentration counts, strengthening payloads, persistence, races/refunds and shared report identity. Web full suite: 187 passed; two additional concentration notice checks and TypeScript passed. Full replay matched the established 185 ok / 1 warn / 13 known fail / 2 unexercised baseline, with no cassette drift (`tmp/quality-replay-source-role.log`). No golden updates, deployment or candidate activation.
